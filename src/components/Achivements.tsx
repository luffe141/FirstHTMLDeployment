import { Box, Container } from "@mui/material";

export default function Achivements() {
  return (
    <Box id="achivements" sx={{ py: 8, bgcolor: "background.paper" }}>
      <Container maxWidth="md"></Container>
    </Box>
  );
}
